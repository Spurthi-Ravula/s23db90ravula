extends layout

block content
  - let items = results
  h1= title
  .container-fluid 
    .row 
      each cin in items
        div(class="col-sm-4 col-md-5 col-lg-3" style=`background-color:lavender;color: blue;font-family: "times";border: 4px solid orange;border-radius: 10px; padding: 15px; border-inline-style: dotted; box-shadow: 4px 4px 4px rgba(1, 3, 3, 0.5)` onclick=`buy(${items.indexOf(cin)})`) 
          p.
           vechicle_type -  #{cin.vechicle_type}
          p.
           color -  #{cin.color}
          p.
           no_of_tyres  -  #{cin.no_of_tyres}
  script.
    function buy(index){
      alert("You purchased item "+index)
    }